version: 2

before:
  hooks:
    - buf generate

builds:
  - id: ores
    main: ./cmd/ores
    binary: ores
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w

  - id: oresd
    main: ./cmd/oresd
    binary: oresd
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w

  - id: ores-wasm
    main: ./pkg/wasm
    binary: ores
    goos: [wasip1]
    goarch: [wasm]
    ldflags:
      - -s -w

kos:
  - id: oresd
    main: ./cmd/oresd
    repositories:
      - ghcr.io/rigsecurity/oresd
    platforms:
      - linux/amd64
      - linux/arm64
    tags:
      - latest
      - "{{.Version}}"
    sbom: spdx

archives:
  - id: ores
    ids: [ores]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

  - id: oresd
    ids: [oresd]
    formats: [tar.gz]
    name_template: "oresd_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: wasm
    ids: [ores-wasm]
    formats: [tar.gz]
    name_template: "ores-wasm_{{ .Version }}"

checksum:
  name_template: checksums.txt

signs:
  - cmd: cosign
    args: ["sign-blob", "--yes", "--output-signature=${signature}", "${artifact}"]
    artifacts: checksum

homebrew_casks:
  - ids: [ores]
    repository:
      owner: rigsecurity
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
      pull_request:
        enabled: true
    homepage: https://github.com/rigsecurity/ores
    description: "Open Risk Evaluation & Scoring — deterministic cybersecurity risk scoring engine"

scoops:
  - ids: [ores]
    repository:
      owner: rigsecurity
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
      pull_request:
        enabled: true
    homepage: https://github.com/rigsecurity/ores
    description: "Open Risk Evaluation & Scoring — deterministic cybersecurity risk scoring engine"
    license: Apache-2.0

nfpms:
  - id: ores
    ids: [ores]
    package_name: ores
    vendor: Rig Security
    homepage: https://github.com/rigsecurity/ores
    maintainer: Rig Security <oss@rig.security>
    description: "Open Risk Evaluation & Scoring — deterministic cybersecurity risk scoring engine"
    license: Apache-2.0
    formats: [deb, rpm]
    section: utils

  - id: oresd
    ids: [oresd]
    package_name: oresd
    vendor: Rig Security
    homepage: https://github.com/rigsecurity/ores
    maintainer: Rig Security <oss@rig.security>
    description: "ORES scoring daemon — HTTP + gRPC via ConnectRPC"
    license: Apache-2.0
    formats: [deb, rpm]
    section: utils
    contents:
      - src: packaging/oresd.service
        dst: /usr/lib/systemd/system/oresd.service
        type: config

changelog:
  sort: asc
  filters:
    exclude:
      - "^chore"
      - "^docs"
      - "^test"
